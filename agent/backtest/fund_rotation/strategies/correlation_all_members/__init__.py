"""correlation_all_members — migration baseline strategy (design §32.2).

Wraps the legacy "selected-cluster all-members equal-weight" behaviour as a
complete FundRotationStrategy so the common Runner can drive it unchanged.
"""
